<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.lala.Mapper.UserMapper">
    <select id="selectById" resultType="com.example.lala.DTO.Response.UserProfile" parameterType="String">
        select user_type       as accountType,
               profilename     as profileName,
               nickname        as userNickname,
               email           as emailAddress,
               password_update as passwordUpdate
        from user
        where user_id = #{userId}
    </select>

    <update id="updateNickname" parameterType="map">
        UPDATE user
        SET nickname = #{nickname}
        WHERE user_id = #{userId}
    </update>

    <select id="getCurrentPassword" parameterType="String" resultType="String">
        SELECT password
        FROM user
        WHERE user_id = #{userId}
    </select>

    <update id="updatePassword" parameterType="map">
        UPDATE user
        SET password        = #{password},
            password_update = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="updateProfileImage" parameterType="map">
        UPDATE user
        SET profilename = #{profileImageUrl}
        WHERE user_id = #{userId}
    </update>

    <select id="checkNicknameDuplicate" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM user
        WHERE nickname = #{nickname}
          AND user_id != #{excludeUserId}
    </select>

    <!-- 사용자 ID로 전체 정보 조회 -->
    <select id="selectUserById" parameterType="String" resultType="com.example.lala.DTO.UserCommon">
        SELECT *
        FROM ` user `
        WHERE user_id = #{userId}
    </select>

    <update id="updateUserType" parameterType="String">
        UPDATE user
        SET user_type = #{userType}
        WHERE user_id = #{userId}
    </update>
</mapper>