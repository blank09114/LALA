<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.lala.Mapper.CommunityReportMapper">

    <!-- 사용자가 해당 게시물을 신고를 했는지를 판단 -->
   <select id="checkUserReport" resultType="boolean">
        select count(*) > 0
        from post_report
        where post_id = #{postId} and report_user_id = #{userId}
   </select>

    <!-- 신고 테이블 추가 -->
    <insert id="addPostReport">
        insert into post_report (post_id, report_user_id, content , report_date)
        values (#{postId}, #{reportUserId}, #{content} ,NOW())
    </insert>

    <!-- 사용자가 해당 댓글을 신고했는지 확인 -->
    <select id="checkUserCommentReport" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM comment_report
        WHERE comment_id = #{commentId} AND report_user_id = #{userId}
    </select>

    <!-- 댓글 신고 추가 -->
    <insert id="addCommentReport">
        INSERT INTO comment_report (comment_id, report_user_id, content, report_date)
        VALUES (#{commentId}, #{reportUserId}, #{content}, NOW())
    </insert>


</mapper>